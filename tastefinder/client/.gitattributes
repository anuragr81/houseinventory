# Regenerated by `make regen-client`; never hand-edited. Marking it collapses
# these files in GitHub's diff view instead of drowning a PR in codegen noise
# -- worth doing now while it's ~560 lines, before more endpoints exist.
packages/tastefinder_api_client/** linguist-generated=true
