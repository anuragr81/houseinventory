# Phase 4 needs one command to regenerate the Dart client. The rest of the
# targets docs/00_BOOTSTRAP.md asks for (setup, test, lint, run-server,
# run-client, migrate) are Phase 5 (developer ergonomics) and arrive there.

.PHONY: regen-client

regen-client:
	./scripts/regen_client.sh
